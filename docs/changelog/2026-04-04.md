---
title: 4月4日更新
date: 2026-04-04
---

### FEAT: 
- 将 `swap` 等待信息接入灵动岛;将“计时开关”信息接入灵动岛
- 新增灵动岛第二行布局规则：只出现 `swap` 或开关其一时居中显示；两者同时出现时左右分布显示。
- 允许灵动岛在没有系统媒体信息时，仍作为 `swap`/计时开关 的承载容器显示。
- 新增`CP`点透明度调整
- 新增锤模式的判断
### FIX: 
- 修复`CP`点`tile`和传送混用导致某些图块消失的`BUG`
- 修正灵动岛尺寸为可动画扩展高度，确保 `swap`/计时开关 出现时胶囊能够平滑向下展开，而不是维持单行高度。
- 将开关追踪逻辑拆分为独立更新与汇总流程，避免只在左下角渲染路径里生效
### DEL: 
- 删除左下角独立的 `swap` 提示渲染。
- 删除左下角独立的开关倒计时渲染。
- 删除 `swap` 必须依赖底部 HUD 才能看到的旧展示路径，统一收口到灵动岛。

→ [4月5日更新](/changelog/2026-04-05)
