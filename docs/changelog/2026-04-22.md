---
title: 4月22日更新
date: 2026-04-22
---

### Added
- 资源页面异步加载优化，皮肤加载分帧处理。
- `QmClient` 设置新增 `Contributors` 页和 `overview`。
- 翻译模块新增 LLM、自动出站翻译、`Provider`、独立 `API Key`、环境变量支持。
- 新增语言按钮图标、`OpenAI-compatible` 后端、`DDNet` 术语 `prompt`、中文检测自动翻译。
- 聊天栏新增翻译按钮 UI，并接入自动出站翻译。
- 聊天光标实现记分板式跟随逻辑。
### Fixed
- 修复翻译 UI 下拉、`endpoint` 展示、并发控制。
- 统一 `LLM API` 选项并设置 `ZhipuAI` 默认 `preset`。
- 修复 `JSON` 请求中的 `model name` 转义。
- 修复聊天打开时光标启用状态。
- 修复 `FTAPI` 固定端点、`GetLlmEndpoint`默认分支、`Provider` 边界校验、目标语言格式校验。
- 修复`translate_llm_provider_test`缺少`config.h`和工具依赖链接。
### Removed
- 去除鼠标相关改动。
- 删除残留 `Rust` 语音模块。

→ [4月23日更新](/changelog/2026-04-23)
