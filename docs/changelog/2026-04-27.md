---
title: 4月27日更新
date: 2026-04-27
---

- 聊天翻译后端警告功能，优化语言菜单逻辑。
- `macOS` 启用` Vulkan via MoltenVK`，并将库打包进 `DMG`。
- 快速练习增强输入处理，添加输入过滤和状态捕获。
- 增强配置管理和本地化。
- `Demo` 截图预览功能，支持重置和同步预览状态。
- 本地化截图和 `Demo` 目录按钮及提示。
- 修复 `macOS Vulkan` 库路径、CI 链接、`codesign` 后 ICD JSON 位置、默认 `Vulkan backend` 配置。
- 修复 `MoltenVK ICD JSON` 放置目录。
- 恢复 `Vulkan` 和 `workshop asset` 加载。
- 删除 `CI copy-to-ddnet-libs` 路径，改由 `CMake` 直接解析 `brew` 路径。

→ [4月28日更新](/changelog/2026-04-28)
