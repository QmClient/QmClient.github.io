---
title: 5月12日更新
date: 2026-05-12
---

### FEAT:
- 消息文字渐变色块上限从 5 个提升到 7 个。
- 地图编辑器 `Quad` 右键菜单新增“缩放 %”，支持按`pivot`等比缩放。
- `Quad` 等比缩放接入 `QuadTracker`，支持撤销/重做。
- 新增`HUD`吸附以及自动对齐
- 坐标对齐提示回归
- 新增“加入QQ群”按钮
### FIX:
- 消息渐变配置迁移到 `QmClient` 配置域，保存到 `QmClient/settings_qmclient.cfg`。
- 修改消息渐变后立即保存配置，避免重启恢复默认。
- 预览背景改为仅宽度减半，高度保持不变。
### DEL:
- 移除旧的 X 对齐弹出提示残留逻辑

→ [5月13日更新](/changelog/2026-05-13)
