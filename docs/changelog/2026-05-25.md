---
title: 5月25日更新
date: 2026-05-25
---

### FEAT:
- 更新工作流清单、`PR` 模板、忽略规则和版本元数据。
- 添加 `QmUi` 动画过渡、弹簧配置、缓动曲线和动画驱动。
- 实现现代菜单标签页，增强开始菜单渲染与按钮反馈。
- 引入 `QmUi Tokens`、按钮、表单、导航、容器等 `UI` 基础组件。
- 增强设置菜单、地图选择器、投票 `UI`、`HUD`/背景等界面表现。
- 改进`Fast Practice`，加入终点传送与武器状态归一化能力。
### DEL:
- 移除旧的悬停缩放按钮反馈和已弃用 `UI` 函数。
